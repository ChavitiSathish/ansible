- name: Demo on Variables
  host: all
  vars:
    URL: google.com
  tasks:
    - name:Print a Varriable
      debug:
        msg: "URL = {{URL}}"
## When there is a variable reference(calling variable), Double quote it
